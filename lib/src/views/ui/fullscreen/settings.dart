import 'package:flutter/material.dart';

Material settings = Material(
  child: Column(children: [
    AppBar(
      title: Text('Settings'),
      actions: [
        TextButton(
          child: Text("APPLY"),
          onPressed: () {},
        )
      ],
    )
  ]),
);
